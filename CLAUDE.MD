# my-ToDoList 앱의 최상위 지침

## 반드시 준수해야할 지침

- 모든 커뮤니케이션은 한국어로 할 것
- 오버엔지니어링 금지

## 참조 문서

| 문서명 | 경로 | 요약 |
|---|---|---|
| 도메인 정의서 | `docs/1-domain-definition.md` | User/Category/Todo 엔티티, 엔티티 관계, 할일 상태(시작전/진행중/완료/지연) 전이 규칙, 핵심 비즈니스 규칙, 예외 케이스, 요구사항 추적 매핑을 정의한 문서 |
| PRD | `docs/2-prd.md` | 목표 사용자, MVP/확장 범위, 기술스택(React 19+TS+Zustand+TanStack Query / Express+pg+PostgreSQL 17), FSD 레이어, 인증(JWT Access+Refresh), 비기능요구사항, 2일 일정 마일스톤, 리스크를 정의한 제품 요구사항 문서 |
| 사용자 시나리오 | `docs/3-user-scenario.md` | 일반 사용자(Member)의 회원가입/로그인/토큰재발급/로그아웃/회원정보수정/Todo 등록·조회·필터·수정·삭제·완료토글 10개 시나리오(사전조건/기본흐름/예외흐름/완료조건) |
| 관리자 시나리오 | `docs/3-admin-scenario.md` | 관리자(Admin)의 회원목록조회/카테고리 등록·수정/카테고리 삭제(참조 Todo 기본카테고리 재할당) 3개 시나리오 |
| 와이어프레임 | `docs/4-wireframe.md` | 8개 화면(회원가입/로그인/Todo목록/Todo등록/Todo편집/회원정보수정/관리자-회원목록/관리자-카테고리관리)의 ASCII 레이아웃, 인터랙션 요소, 반응형(데스크톱/모바일) 설계 원칙과 예시 |
| 프로젝트 구조 설계 원칙 | `docs/5-project-principle.md` | 공통 최상위 원칙, 프론트(FSD: app/pages/widgets/features/entities/shared)·백엔드(라우트-서비스-쿼리) 레이어 원칙, 코드/네이밍 컨벤션, 테스트/품질 원칙, 로깅/에러핸들링 미들웨어를 포함한 설정/보안/운영 원칙, 프론트·백엔드 디렉토리 구조 |
| 아키텍처 다이어그램 | `docs/6-arch.md` | mermaid로 그린 전체 시스템 아키텍처(단일 Express+단일 PostgreSQL), 요청 처리 흐름(미들웨어 포함), 인증 요청 시퀀스(로그인/재발급/로그아웃) 다이어그램 |
| ERD | `docs/7-erd.md` | mermaid erDiagram으로 그린 users/categories/todos 테이블 구조(PK: user_id/category_id/todo_id, 생성자·수정자 포함)와 테이블별 핵심 제약사항 |
| 실행 계획(WBS) | `docs/8-plan.md` | DB/Backend/Frontend 단위로 분할한 Task 목록(각 Task별 수행작업, 체크박스 완료조건, 선행 Task) |
| DB 스키마 DDL | `docs/schema.sql` | ERD 기반 PostgreSQL 17 `CREATE TABLE` DDL(users/categories/todos), 기본 카테고리 유일성 제약, 인덱스 포함 |
| API 명세 | `backend/swagger.json` | 인증/User/Category/Todo/헬스체크 15개 엔드포인트에 대한 OpenAPI 3.0 스펙(요청·응답 스키마, JWT Bearer 인증, 에러 응답 포맷) |

## 작업 원칙 (Andrej Karpathy CLAUDE.md 기반)

LLM이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침이다. 위의 프로젝트 전용 지침과 함께 적용한다.

> **트레이드오프**: 이 원칙들은 속도보다 신중함 쪽으로 기운다. 사소한 작업에서는 상황에 맞게 판단해서 유연하게 적용한다.

### 1. 코딩 전에 먼저 생각하기
**추측하지 않는다. 혼란스러운 부분을 숨기지 않는다. 트레이드오프를 드러낸다.**

구현하기 전에:
- 자신이 세운 가정을 명시적으로 밝힌다. 확신이 없으면 질문한다.
- 해석이 여러 갈래로 갈릴 수 있으면 그 대안들을 모두 제시한다 — 조용히 하나만 골라서 진행하지 않는다.
- 더 단순한 접근법이 있다면 그렇다고 말한다. 필요하면 요청 내용에도 이의를 제기한다.
- 무언가 불명확하면 멈춘다. 무엇이 헷갈리는지 구체적으로 짚고 질문한다.

### 2. 단순함 우선
**문제를 해결하는 최소한의 코드만 작성한다. 추측성 요소는 넣지 않는다.**

- 요청받지 않은 기능은 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 시나리오에 대한 에러 처리를 만들지 않는다.
- 200줄을 썼는데 50줄로 될 수 있다면, 다시 쓴다.

스스로에게 물어본다: "시니어 엔지니어가 보면 이걸 과하게 복잡하다고 할까?" 그렇다면 단순화한다.

### 3. 외과적 수정
**반드시 건드려야 할 부분만 건드린다. 자신이 만든 것만 정리한다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷팅을 "개선"하지 않는다.
- 문제가 없는 부분을 리팩터링하지 않는다.
- 자신의 취향과 다르더라도 기존 스타일을 그대로 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고, 임의로 삭제하지 않는다.

내 변경으로 인해 고아(orphan)가 생긴 경우:
- 내 변경 때문에 더 이상 쓰이지 않게 된 import/변수/함수는 제거한다.
- 원래부터 있던 죽은 코드는 요청받지 않았다면 제거하지 않는다.

판단 기준: 변경한 모든 줄은 사용자의 요청과 직접 연결되어야 한다.

### 4. 목표 지향 실행
**성공 기준을 정의한다. 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성한 뒤, 그것이 통과하도록 만든다"
- "버그 수정" → "버그를 재현하는 테스트를 작성한 뒤, 그것이 통과하도록 만든다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 모두 통과하는지 확인한다"

여러 단계로 이루어진 작업이라면 간단한 계획을 먼저 밝힌다:
```
1. [단계] → 검증: [확인할 내용]
2. [단계] → 검증: [확인할 내용]
3. [단계] → 검증: [확인할 내용]
```

성공 기준이 명확하면 혼자서도 반복(loop)하며 작업을 끝까지 진행할 수 있다. 기준이 모호하면("되게만 해줘") 계속 되물어야 한다.

---

**이 원칙들이 잘 작동하고 있다는 신호**: diff에 불필요한 변경이 줄어들고, 과도한 복잡화로 인한 재작성이 줄어들고, 실수가 생긴 뒤가 아니라 구현하기 전에 명확화 질문이 나온다.
